2. 큐
    선입선출==FIFO -> 대기열. 예약시스템o
    [프로세스] 관리, 시간순서처리

SW==프로그램==어플  ->프로세스
HW
운영체제: 프로그램 -> 실행(프로세스를 할당해줌)

프로세스 처리순서
1. 시간순
2. 우선순위순
**3. 시간순+우선순위순 기다린 시간이 길어질수록 우선순위가 높아짐

Queue 큐
push()==Enqueue()
pop()==Dequeue()
size() empty() full()
front() back()==end();

1.배열
2.연결리스트
[ 0 0 0 0 0 ]
s=0
e=0
[ 5 b c d e ]
s=0
e=0
// 인덱스만으로는 큐자체가
// 비어있는지, 가득찼는지 알수가없음!!
// Dequeue()한 데이터를 0으로 바꿔야함!! 
if(s==e && q[s]==0) >> 텅비어있음
if(s==e && q[s]!=0) >> 가득차있음   