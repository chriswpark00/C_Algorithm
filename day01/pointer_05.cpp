#include <stdio.h>

int main(){
	int arr[5], *p;
	arr[2] = 30; // 10 20 30 40 50 순서로 입력할 예정
	p = &arr[2]; // 배열의 정가운데 인덱스를 포인터에 링킹시킴
	*(p-1) = 10; 
	*(p-2) = 20;
	*(p+1) = 40;
	*(p+2) = 50;
	
	printf("%5d%5d%5d%5d%5d\n", *(p-1), *(p-2), *p, *(p+1), *(p+2));
}
